name: Déploiement de Site Statique

on:
  push:
    branches: ["main"] 

# 🔑 DÉFINITION GLOBALE DES PERMISSIONS
permissions:
  contents: read 
  pages: write 
  id-token: write # <-- LA CLÉ POUR CORRIGER L'ERREUR

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📦 Récupérer le code
        uses: actions/checkout@v4
      
      - name: ⚙️ Configurer GitHub Pages
        uses: actions/configure-pages@v4
        
      - name: ⬆️ Uploader l'Artefact Statique
        uses: actions/upload-pages-artifact@v3
        with:
          path: './' 

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }} 
      
    runs-on: ubuntu-latest
    needs: build
    
    steps:
      - name: 🚀 Déployer sur GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4